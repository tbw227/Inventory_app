global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: inventory-api
    metrics_path: /metrics
    static_configs:
      # Docker Compose: API service name. Local dev on host: host.docker.internal:5000
      - targets: ['backend:5000']
